{
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\" \"docker-compose up -d\"",
    "docker:ps":"docker ps",
    "docker:down":"docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
